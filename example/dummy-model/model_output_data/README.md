# Model output data

The full output (~80 GB of VTK timesteps) is **not stored in git**. It is hosted externally
(Zenodo record 99999, declared in the top-level README front-matter) and appears in the
RO-Crate as an *external entity*.
